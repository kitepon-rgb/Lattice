# RC2 post-publication full CI

- Date: 2026-07-16
- Tested commit: `65a3bb045530029031dd2e84277fcb6680825094`
- Command: `npm run ci`
- Scope: Phase gate full regression、Lattice only

## Result

`npm run test`は168 pass / 0 fail / 0 cancelled / 0 skipped / 0 todo、duration 58,903 msだった。続く
`npm run check`も全syntax入口をerrorなしで完了し、CI processは正常終了した。

このrunはcanonical artifact v2とそのevidenceをcommitしたclean HEADで一回だけ実行した。次を同じfull gate内で再確認した。

- RC2 campaign 5 tests。artifact v2を含むfresh cloneで6 fresh index、plan v3、v1 14-check、v2 15-check、semantic corruption reject。
- fresh Codegraph scope integration。artifact identity 0、live source coverage、oracle affected 3 tests。
- registry-shard transform integrationと6×4 mutation matrix。
- primary 12→0 conflicts／3→1 waves、partial-state 2 waves、capacity 2 waves、third-only typed unknown。
- RC1 transfer normal 3→0 conflicts／2→1 wavesとnegative controlの残存state conflict。
- RC1 v6 canonical artifact 12-check replayと全RC1 regression。

RC2 v1 artifact commit後に観測した旧full gateは163/167 passで、4 failuresはartifact identityのCodegraph混入から同じcampaign fixtureへ
伝播していた。今回の168/168は、tracked exclusion configとidentity v2を含むpost-publication repositoryでそのfailureが再現しないことを示す。

full suiteの再実行は行っていない。dotagents、Observer関連repo、remote、push、publishへの変更はない。
