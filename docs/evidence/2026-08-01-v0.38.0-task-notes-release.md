# Lattice v0.38.0 task notes release evidence

## 公開対象

- package: `@quolu/lattice@0.38.0`
- source commit: `43fd5ac46f894495f0aefe632a14ae8864fd2ff5`
- branch: `main`
- public contract: task-scoped noteを通常の個別ToDo詳細、成功する`todo start`、ローカルGantt右ペインへ
  自動供給し、公開serve／dashboardから本文を除外する。

## 文書とCLI発見面

英日README、CHANGELOG、PLAN、公開製品contract、Gantt設計仕様、文書案内をADR 0149と実装へ揃えた。
`lattice todo --help`と個別helpへ`todo show`、note追記、`note list`を追加し、store非依存のhelp testで固定した。

## release前gate

- task notes／Gantt／CLI help focused test: 50件中50件成功。
- `npm run ci`: product test、sensor test、syntax、CLI surface、open questions、reachability、todo store verifyを含め成功。
- `npm pack --dry-run --json`: version `0.38.0`、package size 5,723,492 bytes、unpacked 59,825,370 bytes。
- 配布物へ`src/todo-note-store.mjs`、`src/todo-cli.mjs`、`bin/lattice-dashboard.mjs`が収載された。
- `git merge-base --is-ancestor HEAD origin/main`と`npm run verify:release-commit`が成功し、
  `43fd5ac46f89`が`origin/main`へ着地済みと確認した。

## npm publish

- npm registry version／latest: `0.38.0`
- tarball: `https://registry.npmjs.org/@quolu/lattice/-/lattice-0.38.0.tgz`
- shasum: `8ff2c9b31464a42694e35f699c9089b8c86df3f5`
- integrity: `sha512-Tz0weKTtHlCS1RpF15IgFVlthEj5pLH6Fb4zSJT8koDVhfPYjeNCE5ISXcwTz+n7LMg25dUstQjpeNAPHo3RLA==`
- file count: 771

## installと常駐実体

- `npm install -g @quolu/lattice@0.38.0`成功。
- `/opt/homebrew/bin/lattice --version`: `0.38.0`
- global package: `/opt/homebrew/lib/node_modules/@quolu/lattice@0.38.0`
- dashboard health: schema `lattice.todo_dashboard_health.v1`、PID 29039、port 62215、version `0.38.0`。
- `launchctl kickstart -k gui/501/dev.kitepon.lattice.bridge`でbridge PIDを1045から73657へ載せ替えた。
  再起動直後のprobeはlisten前でHTTP 000、bounded retry後にlocalhost `127.0.0.1:53939`がHTTP 200となり、
  `lattice bridge status --json`も`reachable: true`を返した。

## 公開後smoke

- `https://lattice.kitepon.dev/`: HTTP 200
- `https://lattice.kitepon.dev/projects/`: HTTP 200
- `https://lattice.kitepon.dev/projects/lattice/`: HTTP 200
- install済みCLIの`todo show --plan todo-task-notes --task ttn-007 --json`が、`note list`なしで
  note本文1件、note head、overflow、全履歴commandを返した。
- 同じ実note本文を公開HTMLで固定文字列検索し、`public_note_body_leak=false`を確認した。

## 非実施

GitHub Release／source tag作成とCloudflare tunnel設定変更は依頼範囲外のため実施していない。
