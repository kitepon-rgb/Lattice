# v0.6.7 project discovery／初期authoring公開証跡

- Date: 2026-07-20
- Implementation commit: `81bf35ba51f6fc6209761285a7966cde5cf534bb`
- Package: `@quolu/lattice@0.6.7`
- Registry tag: `latest`

## 受入

- `lattice status --json`はCLI version、git project、canonical store、active plan/run、
  `uninitialized | ready | active_run | invalid`、`can_create_plan`、次commandを
  `lattice.project_status.v1`で返す。
- storeなしGit repoはexit 0の`uninitialized`で、`.lattice/`の存在を接続markerへ使わない。
- `lattice plan create --input <ref>`はcanonical `lattice.plan_create_input.v1`から
  `.lattice/todo`を一transactionで初期化する。
- `lattice plan create --schema --json`でNPM package収録済みschemaを一行取得できる。
- 不正storeはtyped `invalid`でfail closedし、Markdownへfallbackしない。

## 検証

- focused CLI/store tests: green
- SHA-256 Git、末尾空白repo root、親`.lattice` symlink、missing／noncanonical／上限超過input:
  green
- valid manifest改変、stale snapshot、rename後exact再検証とrollback: green
- `npm run ci`: green
- sensor suite: 147 files passed、3 skipped、2488 tests passed、37 skipped
- 独立refuter最終監査: 通常運用で再現する未解消契約欠陥なし、公開可
- `npm pack --dry-run`: 785 entries。`src/project-cli.mjs`と
  `docs/schemas/lattice.plan_create_input.v1.schema.json`の収録を確認

## 公開後gate

- registry shasum: `b40e224d48f0e382bc74fae2f16e2b86938ccd42`
- registry integrity:
  `sha512-F9H1S7TVcqtz+CPUG1i2ZzPWjculM8gZ7QyzQYEickuEPqGNC8id0DQiXizVtIempYM0wwWJtB/HEOt//ofLog==`
- candidate SHA-256:
  `77d0e341b66921a0e474a2823f8379e07a2b0c551f888e4d7f3122a0a01eabef`
- global executable: `/opt/homebrew/bin/lattice`
- `lattice --version`: `0.6.7`
- Lattice repo smoke: `state=uninitialized`、`can_create_plan=true`、schema command green
- dotagents smoke: `state=active_run`、8 active plans、`todo verify` green、
  `snapshot_stale=false`

global rollbackは`npm install -g @quolu/lattice@0.6.6`。公開物は上書きせず、修正が必要なら
新しいpatch versionを発行する。
