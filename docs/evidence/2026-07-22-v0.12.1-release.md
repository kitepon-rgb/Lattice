# Lattice v0.12.1 release evidence

## 対象

- release commit: `a128e1bc9f31a55ed23a1fc0cd6715ce87b1f4b7`
- npm package: `@quolu/lattice@0.12.1`
- release日: 2026-07-22

## 閉じた公開不具合

- crash後にstale daemon descriptorだけが残ったbridge再構成を`not_running`へ収束させ、公開経路を復旧した。
- actor環境のない`lattice status --json`でもproject activityを登録し、既存sessionのprojectが一覧から欠落する問題を修正した。
- Node 26 test workerが注入する`FORCE_COLOR`をCLI fixtureの子processへ継承せず、JSON-only stderrへ警告が混入しないようにした。

## 検証

- bridge／project discovery／dashboard関連focused test: 42/42 成功。
- CLI JSON child環境の再現条件を含むfocused test: 48/48 成功。
- `npm run ci`: exit 0。
  - current product gate: 75 suites。
  - bundled sensor: 139 files成功、3 files skipped、2192 tests成功、37 tests skipped。
  - syntax check／project identity check: 成功。
- `npm pack --dry-run`: 727 files、package size 5.5 MB、unpacked size 58.9 MB。
- `npm publish`: `@quolu/lattice@0.12.1`を`latest`へ公開成功。
- `npm install -g @quolu/lattice@0.12.1`: 成功。
- global `lattice --version`とregistry `npm view`: ともに`0.12.1`。
- global CLIによるLattice／Patentの`lattice status --json`: 成功。
- `lattice todo verify --json`: `snapshot_stale=false`、`reconciliation_state=reconciled`。

## 公開smoke

次のURLを外部HTTPS経路から確認し、すべてHTTP 200とproject固有titleを得た。

- `https://lattice.kitepon.dev/projects/`
- `https://lattice.kitepon.dev/projects/aishell/`
- `https://lattice.kitepon.dev/projects/lattice/`
- `https://lattice.kitepon.dev/projects/lattice-patent-jp/`

bridgeは`192.168.1.102:53939`へ明示bindし、dashboard descriptorをupstreamとして解決している。
