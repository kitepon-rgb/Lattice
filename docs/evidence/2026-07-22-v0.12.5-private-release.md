# v0.12.5 private release evidence

## 対象

- source commit: `fbf68b5`（工程表と自律索引契約を修正する）
- package: `@quolu/lattice@0.12.5`
- release date: 2026-07-22 (Asia/Tokyo)

## 修正

- dashboardのactivity TTLが切れても、project storeの`active_set`が非空ならactive project一覧へ残す。
- stale project storeの読取失敗はproject単位で一度だけstructured stderrへ記録し、他projectの表示を止めない。
- 未索引projectでは、AIが反復Read／Grep削減効果と一回限りの索引費用を比較し、workspace書込とshell実行が許可されていれば`lattice sensor init <projectPath> --json`を自律実行できる契約へ変更した。
- MCP initialize instructions、未索引tool応答、CLI guidance、installer instruction、製品契約を同じ判断へ揃えた。

## 検証

- focused dashboard: 10 tests passed。
- focused MCP unindexed policy: 11 tests passed。
- `npm run ci`: exit 0。
- bundled sensor: 139 files passed / 3 skipped、2,192 tests passed / 37 skipped。
- `npm pack --dry-run --json`: 727 files、5,466,029 bytes packed、58,897,285 bytes unpacked。
- `git diff --check`: pass。

## npm publish / install

- registry version: `0.12.5`
- shasum: `1c968f4f86a2dc82517d28d255f22c26d0477187`
- integrity: `sha512-DhGX9M4F/QmIHLqe8uZPJNqtR/6MKuxyR452KNgYWf5JCp1NHuw6wH51GetkLJnLDaj2jKOwqaEOJUGJqq7A+w==`
- `npm access get status @quolu/lattice --json`: `private`
- unauthenticated registry GET: HTTP 404
- global install後の`lattice --version`: `0.12.5`
- installed `sensor/dist`で自律init guidanceを確認し、旧`Indexing is the user's decision`／`don't run it yourself`は不在。

## production smoke

- 旧dashboard PID `17077`をdescriptorとhealthのPID・port一致で照合してからSIGTERMし、v0.12.5で再起動した。
- 新descriptor / health: PID `61959`、port `54613`、project ids `aishell`, `lattice`, `lattice-patent-jp`。
- `https://lattice.kitepon.dev/projects/aishell/`: HTTP 200。
- アプリ内ブラウザで公開project一覧を再読込し、AIShell、Lattice、lattice-patent-jpの3件表示を確認した。
