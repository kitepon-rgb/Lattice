# Lattice v0.39.1 敵対的監査修正 release evidence

> **撤回済み（終端受理の根拠に使用禁止）**
>
> 公開後の独立実装再監査で、通常revisionへ互換入力された`acquire_phase`が既存`design_memo`の
> 置換を比較しない残存経路を確認した。v0.39.1は公開・install・smokeの事実記録としてだけ保持し、
> 修正と再releaseの証拠は`2026-08-01-v0.39.2-adversarial-audit-release.md`を正とする。

## 公開対象

- package: `@quolu/lattice@0.39.1`
- source commit: `0e2c9b451dc26e4201463f48c4ce28a728e60fb0`
- source branch: `main`
- 修正範囲: 監査未受理工程の表示保持、右ペイン設計メモ、AI向けauthoring診断、設計メモ改訂carry、
  migrate整合性、静的Gantt退役、関連文書と回帰試験。

## release前gate

- 各修正直後のfocused test: 48件、73件、103件がそれぞれgreen。
- RC3 dogfood fixtureの欠落していたSensor依存辺を静的importで復元し、関連test 17/17 green。
- 実daemon統合試験の共通cleanup helper適用後、focused test 5/5 green、直後のorphan reaperは
  `matched: 0`、`still_alive: 0`。
- 最終`npm run ci`: exit 0。製品test、Sensor test、static／reachability／todo-store gateを完走。
- CI直後のorphan reaper: `matched: 0`、`still_alive: 0`。
- `npm pack --dry-run --json`: package size 5,730,889 bytes、unpacked 59,875,801 bytes、773 files、
  `sensor/dist/bin/lattice-sensor.js`を収載。
- `0e2c9b4`を`origin/main`へpushし、publish前に公開対象commitが`origin/main`の祖先であることを確認した。

完全gateまでに露出した失敗と対応は
`docs/evidence/2026-08-01-v0.39.0-adversarial-terminal-audit.md`へ原因別に残した。失敗したrunを
最終greenへ丸めていない。

## npm publish / install

- `npm publish --access public`: 成功。`latest`として`@quolu/lattice@0.39.1`を公開した。
- registry tarball: `https://registry.npmjs.org/@quolu/lattice/-/lattice-0.39.1.tgz`
- registry shasum: `dc869c5517bf9e9608ebc50eaa34bfb84cc2e8de`
- registry integrity:
  `sha512-YqAfJ9/q6lZvbxDTRCyHcNzWjLM1uxXNlrEXm7L5TKfDuXZpIHO2TO4uKlJuW9vcr+YOMHRIk8XUQYK3OR6IRw==`
- registry fileCount: 773、unpackedSize: 59,875,801 bytes。
- publish直後の初回global installはregistry metadata伝播前で`ETARGET`。`npm view`でversion、shasum、
  integrityが公開済みになったことを確認後、同じ正規経路を再実行した。
- `npm install -g @quolu/lattice@0.39.1 --prefer-online`: 成功。
- `/opt/homebrew/bin/lattice --version`: `0.39.1`。

## 常駐実体

- `launchctl kickstart -k gui/501/dev.kitepon.lattice.bridge`: 成功。
- bridge: `127.0.0.1:53939`、`reachable: true`。
- dashboard health: schema `lattice.todo_dashboard_health.v1`、version `0.39.1`、port `55703`。
- Hook作業中のローカルmainは未commit bytesを持つためcheckoutを変更しなかった。公開Latticeだけは
  `lattice todo dashboard adopt --json`でpush済み隔離worktreeへ明示移管し、現行storeを表示した。
  Hook側がmainを追従した後にmain rootへ再adoptするまで、この移管は一時運用であり終端受理の前提条件とする。

## 公開後smoke

- `https://lattice.kitepon.dev/`: HTTP 200。
- `https://lattice.kitepon.dev/projects/`: HTTP 200。
- `https://lattice.kitepon.dev/projects/lattice/`: HTTP 200。
- `https://lattice.kitepon.dev/projects/bingo/`: HTTP 200。
- Lattice公開画面は`audit-01`と`audit-04`を「完了」、`audit-05`を「作業中」と表示し、
  `terminal-audit`、`設計メモ`、同一ページ展開バッジを含む。
- Bingo公開右ペインは未着手`retest-08-wave4-improved-diagnostic`へ、前提、1,000人cold seed実行方針、
  原因7分岐、停止点を含む設計メモ本文を表示する。
- Bingo公開HTMLに`docs/bingo-gantt.html`／`bingo-gantt.html`参照は無い。

## 未完了gate

- Hook作業完了後、ローカルmainを`origin/main`へ追従し、dashboard rootをmainへ戻す。
- 本証拠をcommitして同じCriticで実装修正と公開結果を再監査する。
- 契約重大findingが無いことを確認後だけ、terminal review／acceptとrelease ToDoのdoneを記録する。
