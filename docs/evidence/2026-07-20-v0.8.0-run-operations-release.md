# Lattice v0.8.0 run operations release evidence

- Date: 2026-07-20
- Release commit: `1f79182265640bf9dc1a58a4ac9f3426fb99cc10`
- Package: `@quolu/lattice@0.8.0`
- npm shasum: `e44d53068a19420923dbb6df53d4ae2dd2c78ae2`
- Contract: ADR 0060

## Implemented

- project-local ignored store `.lattice/runs/<run-id>`へGit root基準で固定
- `run list --json`、`resume`、`close`、`abandon`をtyped lifecycleとして追加
- store発行とevent更新をatomic化し、lifecycle writeを排他化
- repo外ref、旧`research/runs/rc3`、symlink ancestor、stale base、破損chainをfail closed化
- runtime/control evidence timestampを実在暦日のcanonical UTC millisecondsへstrict化

## Verification

- focused: 81 passed / 0 failed
- Lattice root suite: green（stale sensor indexを正規`lattice sensor sync . --json`後に再実行）
- bundled sensor: 2414 passed / 37 skipped / 0 failed
- syntax gate: exit 0
- pack: 705 entries、`src/runtime-cli.mjs`と`src/timestamp-contract.mjs`を同梱、public `codegraph` bin 0、`lattice-sensor` bin 1
- global install: `lattice --version` = `0.8.0`

## Real host smoke

dotagentsの実repoと現在HEADで`lattice-run-ops-smoke-20260720`を開始した。`run list`でactiveを列挙し、
dotagents Orchestrate SessionStart advisoryが同じrun idを表示した。続けて`run resume`が
`dispatchable: [TA, TC]`を返し、`run abandon --reason smoke_complete`、`event verify`を通過した。
最終`run list --json`は`active_runs: []`。外部provider dispatchは行っていない。

