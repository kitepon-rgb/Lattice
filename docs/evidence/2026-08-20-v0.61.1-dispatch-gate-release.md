# v0.61.1 dispatch往復廃止の公開証跡

- 実施日: 2026-08-20
- Decision: [ADR 0180](../adr/0180-dispatch-round-trips-are-advice.md)
- release commit: `3f106a0537e6d77bdfd8050aeffcc41313b8726f`
- version: `0.61.1`（0.61.0の往復廃止 + Windows attach 修正 + 公開文書追従）

## 受入matrix

| 受入 | 実測 | 結果 |
|---|---|---|
| GitHub product CI（0.61.0 commit `1facb5b4`） | macos-native / linux-native / windows-native の product full test が success。WSL2 は queued のまま | pass（3 OS） |
| 局所静的gate | `npm run check`、`check:cli-surface`、`check:open-questions`、`check:reachability`、`todo verify` | pass |
| Sensor | `npm run test:sensor` 162 files / 2485 tests | pass |
| production audit | `npm audit --omit=dev --audit-level=high` 0 vulnerabilities | pass |
| 既定ブランチ | `3f106a05` は `origin/main` の祖先。`verify-release-commit` 成功 | pass |
| npm公開 | `@quolu/lattice@0.61.1` public。`latest` も 0.61.1。shasum `6fb96fdc54759fbfa383dd3afe785f075884f84a` | pass |
| GitHub Release | [v0.61.1](https://github.com/kitepon/Lattice/releases/tag/v0.61.1) | pass |
| global install | `/opt/homebrew/bin/lattice --version` が 0.61.1 | pass |
| 案内 | インストール済み `lattice todo --help` が「readyならflagなしで着手」「門ではない」 | pass |
| bridge | runtime 0.61.1、heartbeat accepted、reachable true、runtime_drift [] | pass |
| 公開面 | `https://lattice.kitepon.dev/projects/` と `/projects/lattice/` が HTTP 200。title `Lattice — Lattice 依存工程図` | pass |

局所 `npm test` は 2 回とも別件 1 fail（evidence prefetch 注釈 / `ADAPTER_CONTROLLER_UNAVAILABLE`）。どちらも focused 再実行は green。GitHub macos/linux/windows の 0.61.0 product test が正の通し証拠。

`lattice status --json` はこの作業dirで `project_root_conflict`。registry は触っていない。
