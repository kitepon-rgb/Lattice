# Lattice 0.10.0 ready frontier release evidence

- 実施日: 2026-07-21
- release commit: `896d64bbb7f5da400f01a29a4f2f60b0f97633ec`
- package: `@quolu/lattice@0.10.0`
- npm dist-tag: `latest`
- npm shasum: `ae17e5e9c6740f87c29613d67c8a5ce670bd1244`
- npm integrity: `sha512-zHxkAo9RWUHlbrPp79kHrn308REwe3Srn28Guw46933ubGN3Kv1UZ8H50PUEnis0Qkx77PRPkGqXoCkzpQ/HhQ==`
- tarball: 709 files / 58,344,901 unpacked bytes

## 公開物

`npm pack --dry-run --json`とpublish noticeを照合し、次を確認した。

- `src/todo-status.mjs`、`src/todo-cli.mjs`、`src/todo-gantt-html.mjs`、
  `src/todo-gantt-layout.mjs`を収録する。
- private runtime入口`sensor/dist/bin/lattice-sensor.js`を収録する。
- 公開`codegraph` binは0件で、旧runtimeへfallbackしない。
- registryのfile count、unpacked size、shasum、integrityはpublish artifactと一致する。

## 検証

- root product gate: 470/470 pass。
- bundled sensor: 2,187 pass / 37 skip。
- `npm run check`: pass。
- `git diff --check`: pass。
- pack後の必要surface収録と旧公開runtime不在: pass。

## 公開版インストールsmoke

`npm install -g @quolu/lattice@0.10.0`を実行し、PATH上の
`/opt/homebrew/bin/lattice`が`0.10.0`を返すことを確認した。

- `lattice status --json`: `state=ready`、`cli.version=0.10.0`。
- `lattice todo status --json`: `lattice.todo_status_result.v4`と
  `lattice.todo_dispatch_frontier.v1`を返した。
- `lattice todo verify --json`: `snapshot_stale=false`、active planを検証した。
- `lattice todo gantt status`: renderer変更による`stale`を検出後、公開版で再生成して`current`へ戻した。

現在のLattice planは完了済みでready taskがないため、実repo smokeの推奨同時数は0である。複数ready時の
plain start拒否、`--parallel-frontier`、直列化理由は製品testの隔離fixtureで検証し、実repoの完了済み
工程をreopenして公開確認を捏造していない。

## 結論

GitHub release commit、npm `latest`、ローカルglobal installは同じ0.10.0実装へ揃った。
Phase境界・監査回数・ToDo DAGを増やさず、複数readyの最初の着手に並列方針または直列化理由を要求する。
