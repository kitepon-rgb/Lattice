# Lattice v0.12.2 private release evidence

## Release

- package: `@quolu/lattice@0.12.2`
- access: `restricted` / npm account status `private`
- dist-tag: `latest -> 0.12.2`
- source commit: `3fc2f39`（`Lattice 0.12.2をprivate再公開可能にする`）
- global install: `lattice --version` = `0.12.2`

0.2.0〜0.12.1は特許出願前の公開停止対応でunpublish済みのため再利用せず、同じ機能を0.12.2としてprivate再公開した。`publishConfig.access`も`restricted`へ変更し、後続publishが意図せずpublicへ戻らないようにした。

## Registry verification

- authenticated `npm access get status @quolu/lattice --json`: `private`
- authenticated `npm view`: versions `0.1.0`, `0.12.2`; `latest` = `0.12.2`
- unauthenticated registry metadata request: HTTP 404 / `{"error":"Not found"}`

## Verification

- `npm pack --dry-run --json`: 0.12.2、727 entries、restricted manifestを生成。
- product full suite: 685/686 green。2,000 tasks / 8,000 edgesのlayout性能試験だけがsuite競合下で14.9秒となり10秒予算を超過。
- `node --test test/todo-gantt-layout.test.mjs`: 12/12 green。同じ性能試験は単独1.82秒。
- sensor full suite: 2191/2192 green。MCP initialize testの終了時一時directory cleanupが`ENOTEMPTY`で競合。
- `npx vitest run __tests__/mcp-initialize.test.ts --maxWorkers=1 --minWorkers=1`: 3/3 green。
- `npm run check`: green。
- `npm run check:project-identity`: green。

2件のfull-suite失敗はいずれも機能コードの失敗ではなく、同一fixtureの単独再実行で再現しなかった。version、private publish既定、Changelog以外の製品コードは0.12.1から変更していない。
