# v0.58.2 未完了coverage一致修理・公開証跡

## 再現

Peertableの既存planへ途中適用した。`structure input --dry-run`はpending／in-progress／blocked taskを
全件列挙したsourceをvalidとしたが、同じsourceのcompileは完了済みtaskを
`topology_only_task_ids`として`STRUCTURE_COVERAGE_MISSING`にした。

## 修正

- compile／finalizeへplan全taskの状態を渡す。
- structure set外のtopology taskは、状態がdoneの時だけcoverage findingから除外する。
- structure set自身のtask状態欠落、未完了topology taskの漏れ、未登録taskは従来どおり拒否する。
- realization、finalization、structure schemaは変更しない。

## 検証

| 面 | 結果 |
|---|---|
| focused | structure overlay／CLI 21件通過。完了済みtopology taskの正例と未完了task漏れの負例を両方固定 |
| 完全CI | `npm run ci`通過 |
| Sensor | 147 files、2,308 tests通過、183 skipped |
| 静的面 | syntax 169 files、CLI surface 68 commands、reachability 115 modules |
| audit | root／Sensorのproduction依存とも脆弱性0 |
| pack | 826 files、7,483,989 bytes、unpacked 78,075,509 bytes |
| npm | `@quolu/lattice@0.58.2`をpublic publish、`latest=0.58.2` |
| runtime | global CLI／bridge runtime 0.58.2、heartbeat accepted、reject／reclaim 0 |
| Lattice公開dashboard | Peertableプロジェクトの7 planを構造検査面へ表示。6 planはfresh consistent、finding 0 |

Peertableの`peertable-dm-delivery-fx4e-20260811`は0.58.1で先行compile済みで、構造source commitにより
current HEADが進んだためfreshnessだけstaleになった。保存artifactのcompiled verdictはconsistent、finding 0、
入力済みかつenabledである。immutable bindingを再発行せず、task完了時のrealize／finalizeで更新する。

## 公開同一性

- release commit: `956dbf841b0ec1e50ddd18ec75be8e06ff586b4b`
- npm SHA-1: `7473be3cb034892c575380e10bd5517b3b2f5e95`
- npm integrity: `sha512-CMqHsmZVYywaI+XwB9/QjKoqta/MVoRQceJiFqyaVPIhqM6NUg5uSBF6adDZjSUGxY8MWlHEX5S+2ZY3YjwMJA==`
- rollback: `@quolu/lattice@0.58.1`をglobal installしbridgeを再起動する。

## 自動化候補

未完了task列挙、canonical化、digest、plan反復、dry-run／input／compile集計、version整合、CI、publish後の
install／restart／smokeは機械処理できる。AI判断を残すのは論理dataflow設計とfindingの原因分類だけである。
