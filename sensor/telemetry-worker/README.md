# 旧上流telemetry worker（未使用・未配布）

このdirectoryはfork元の実装由来を保持する履歴sourceである。Latticeはこのworkerをbuild、deploy、
公開、接続しない。現行runtimeは外部networkへtelemetryを送信せず、endpointも所有しない。

現行契約は[`../TELEMETRY.md`](../TELEMETRY.md)を参照する。ここに残るpackage名やURLを
Latticeの設定・配布・運用へ使用してはならない。
