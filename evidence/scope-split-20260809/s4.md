# s4 実行層の通知接続 — 実装・検証証跡

## 結論

`prediction_excess`を捨てず、計画層の`scope_expanded`と同じfield集合へ正規化して、
actual dogfood・Stage 1 dogfood・scripted campaignの公開戻り値まで伝播した。
単独の予測超過を`findings`へ入れず、`conflict_found`／`intake_frozen`を生成しない既存挙動は維持した。

## 実施

- 実装commit: `adba450`
- `src/runtime-engine.mjs`
  - runtime manifestの`writes`を初回予測として数える。
  - 単独`undeclared_write`のpath／resourceを`added_paths`へ正規化する。
  - `task_id`、比較digest、初回・現在path数、増分、回数、gate形を持つ`scope_expanded` entryを返す。
  - 重複pathは`Set`で1件へ畳み、停止判断の分岐は変更しない。
- `src/rc3-actual-dogfood.mjs`、`src/rc4-stage1-dogfood.mjs`、`src/rc3-scripted-campaign.mjs`
  - classification結果の`scope_expanded`を各runtime driverの返却面まで渡す。
  - scripted campaignはartifact条件とcampaign返却値へも同じsummaryを載せる。
- focused test 4本で正側・負側・重複再分類を固定した。

## 検証

- `node --test test/rc3-runtime-engine.test.mjs`: 16/16 green
- `node --test --test-name-pattern='予測外write' test/integration/rc3-worktree-executor.integration.mjs`: 1/1 green
- `node --test test/integration/rc4-stage1-dogfood.integration.mjs`: 2/2 green
- `node --test --test-name-pattern='8条件' test/integration/rc3-scripted-campaign.integration.mjs`: 1/1 green
- `npm run check`: syntax 149 files green
- `git diff --check`: green

独立監査はroom [1341]。実diff読解、関連4 test 34/34、2 predecessorの`gate_shape:true`負側、
再分類時のsummary同一・event増分0、重複path一意を確認し、defect-free・close可と判定された。

## スキップ

- full regressionは実施していない。変更に直結する4 testと標準syntax gateで閉じた。
- push／publishは依頼範囲外かつproject規約で明示指示が必要なため実施していない。
