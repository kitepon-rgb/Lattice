# s6 受入gate実測

## 対象

- managed run: `t19-scope-split-20260809-2134`
- 受入用実plan: `scope-split-live-20260809`
- split successor: `rev-e5ef810120268b23c8b3f240`
- revision digest: `733a6f178b85307fafc8ec61c6a90e7c11c19dac6ec8a961e25e4cad31026892`

## 受入結果

1. 宣言拡張から勧告まで
   - 初回compileはA/B/Cすべて `growth_events=0`、recommendation 0。
   - Aの境界を1 pathから3 pathへ拡張したcompileは、Aだけに `growth_events=1`、
     `first_seen_path_count=1`、`path_count=3` と
     `scope_expanded_consider_split` を返した（result `5e9f0c4153671ea5ba537b24056e5544c7fbef49c5060d279dc792da3024057c`）。
   - `todo start A` のadvisoryにも同じ勧告がtask限定で載った。

2. 実taskのsplit
   - split result: `62dd4d20f3866eef2634393052193d7afecfad196fcb709a3b76be469872ad91`。
   - residual Aは直後にpendingへ戻り、A1/A2を新規抽出した。
   - hard dependencyは `A1→A2`、`A1→A`、`A2→A`、`A→B`。
     旧下流Bはresidual A経由の依存を維持した。

3. lineage
   - A1とA2の `parent_task_id` はともにA。
   - residual A、下流B、負control Cの `parent_task_id` はnull。

4. 負のコントロール
   - 境界を拡張していないCの `todo start` は
     `scope_expansion_recommendations: []`（result `08781349353af8c73d4d55d9111a25191a14b9080e21975d572b5f31cb5b0d1a`）。

5. 後方互換
   - 既存plan `scope-split-20260809` はs2でcompile・start・doneを無変更で完走済み
     （done commit `ce2efa5`）。
   - split successorもwitnessを実態へ束縛後、compileは
     `outcome=compiled`、unknown 0、conflict 0
     （result `0a5f1a1360231e88539d0740e32ef850246a8e09b81dfe776bbc5add4fc6357a`）。
   - A1→A2→residual A→BとCを通常のstart/done入口で完走し、
     `todo verify --plan scope-split-live-20260809 --json` はgreen
     （result `83a05ce5ac2dea1e4f95e865a14ee23ef29d4bcbffa484df84a6fa8fdd1be240`）。

## 実行中に露出したtyped fail

- repo外のmigration inputは `INPUT_OUTSIDE_REPOSITORY` で無変更拒否。
- worktreeのdashboard root競合は `PROJECT_ROOT_CONFLICT` で無変更拒否。
  `LATTICE_DASHBOARD_AUTOSTART=0` の非配信モードで実測を継続した。
- 既存sourceをarchive_refへ指定したproposalは
  `source_cutover_archive_exists` で無変更拒否。
  新規 `.lattice/todo/archive/scope-split-live-20260809.md` へ修正した。
- managed worktree固有sensorを `lattice sensor init` して
  `worktreeMismatch=null`、pending changes 0を実測後にcompileした。

## 結論

s6の固定された5受入条件はすべて実測で充足した。split判断はAI側に残り、
Latticeは膨張記録・助言・revision・lineage・依存付替え・検証だけを担当している。
