# s2 宣言膨張advisoryの証跡

## 成果

- helper/test commit `95f8705` は、記録済み `scope_expanded` のうち
  `growth_events` がsafe integerかつ1回以上のtaskだけを、分割検討の助言へ写す。
- CLI/test commit `4ce0eed` は、`todo start`のadvisoryへ当該taskだけの
  `scope_expansion_recommendations`を添付し、記録なし・膨張ゼロでは`[]`を返す。
- independence compile結果はshape変更をin-place追加せず
  `lattice.todo_independence_compile_result.v2`へ上げ、同じ助言を返す。
- 助言はresultへ添付するだけでstart/compileの拒否条件へ入れず、分割判断はAIに残した。
- gate形は依存合流点という構造事実を文面へ添えるが、severabilityや自動分割を主張しない。

## 検証

- `node --test test/todo-independence-guidance.test.mjs test/todo-independence-cli.test.mjs`:
  37/37 green。
- 実sensorを初期化したfixtureで、前回1 pathから今回2 pathsへの宣言膨張をcompileし、
  result v2へ`growth_events: 1`、`first_seen_path_count: 1`、`path_count: 2`の助言が出た。
- start advisoryはT1の既知膨張2回をT1だけへ返し、T2の膨張ゼロとartifact欠落は`[]`。
- 初回focusedでは、対象advisory fixtureへ膨張値が入っていない負側と、sensor生成物を
  ignoreしないdirty fixtureがそれぞれredになった。対象値と`.lattice/` ignoreを正してgreenへ戻し、
  助言が無い旧形とclean-worktree契約違反を測定器が検出する感度を確認した。
- `npm run check`: syntax 150 files green。
- `git diff --check`: green。

## 独立監査

Yuzuが同じscope-split工程の独立監査席として`95f8705`と`4ce0eed`の実diffだけを照合した。
safe integerかつ`growth_events > 0`だけの助言化、null/0の除外、canonical task IDでの
task別filter、記録なし`[]`、start resultへの非拒否添付、compile result v2を確認。
両commitの`git show --check`、両sourceの`node --check`、focused 37/37はgreenで、
残存findingなし（room `[2139]`）。

## 判定

s2の受入条件を満たす。s6の実plan受入へ進められる。
