# g1 工程表へtask_id表記を出す

## 結果

HTMLの全工程一覧・前提／後続表示とSVGカードの可視・読み上げ表記を、連番ではなくstoreの`task_id`を一次表記とするよう統一した。presentation由来の旧連番は`data-task-number`、`data-task-number-normalized`、`data-task-number-globally-unique`として保持し、既存の機械利用を壊していない。

## 変更

- `src/todo-gantt-html-shared.mjs`: 共通task参照を`工程 <task_id>`へ変更。
- `src/todo-gantt-svg.mjs`: 可視ラベル、aria-label、titleをtask_idへ変更。旧番号data属性は維持。
- `test/todo-gantt-render.test.mjs`: HTML/SVGのtask_id一次表記と旧番号属性の共存を固定。
- `test/todo-gantt-nested.test.mjs`: 親なしSVGのtask_id表記を明示assertし、意図したラベル差分だけgoldenを再封印。

実装commitは`4b5b45e`、入れ子回帰修正は`0a2ac58`。witnessは`c89b424`で実変更pathへ拡張し、同HEADで再compileした結果はcoverage=`verified`、task 1、conflict 0、unknown 0。

## 最終gate

```text
node --test test/todo-gantt-independence.test.mjs test/todo-gantt-nested.test.mjs test/todo-gantt-presentation.test.mjs test/todo-gantt-render.test.mjs test/todo-gantt-selfcontained.test.mjs
tests 56 / pass 56 / fail 0

npm run check
syntax check passed: 149 files
```

独立再監査はAkari `[1457]` とNagi `[1466]` が`c89b424`／`0a2ac58`を含む関連5 suites 56/56 green、task_id一次表記と旧番号属性維持、追加欠陥なしを確認した。

## 実ブラウザ受入

一時サーバ`http://127.0.0.1:47840/projects/lattice/`を実ブラウザで確認した。

- SVGカード: `作業中 · 工程 g1`、aria-labelは`工程g1。作業中…正規ID gantt-labels-20260809/g1`。
- 全工程一覧: `工程 g1`と全文タイトルを同じ行に表示。
- 互換属性: `data-task-id="g1"`、`data-task-number="1"`、`data-task-number-normalized="1"`を同時保持。
- 長いtask_idを含む他カード、依存線、右ペインに視認できる崩れなし。

検証後にブラウザtabをfinalizeし、一時サーバを停止した。port 47840のLISTEN processが0件であることを確認済み。
