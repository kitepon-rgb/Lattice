# v2 公開Gantt親子表示の実ブラウザ受入

- 実施者: yuzu
- 実施日: 2026-08-09
- 対象plan: `scope-split-viz-20260809`
- 対象task: `v2`
- 実データplan: `scope-split-live-20260809`
- active revision: `rev-e5ef810120268b23c8b3f240`
- split result: `62dd4d20…`
- revision result: `733a6f17…`

## 実行面

run `t19-scope-split-20260809-2134` の隔離worktreeから、dashboardの自動起動を無効にして公開Ganttを起動した。

```text
LATTICE_DASHBOARD_AUTOSTART=0 node bin/lattice.mjs todo gantt serve --port 0 --scope all
http://127.0.0.1:61159/projects/lattice/
```

Browser pluginのChrome extension bindingで上記URLを開き、実live planのDOMと画面を操作した。受入後はbrowser tabをfinalizeし、serve processをSIGINTで停止してshell復帰を確認した。

## 実測結果

1. `scope-split-live-20260809/A`だけが`data-nested-toggle-for`を持ち、初期状態は`aria-expanded=false`だった。
2. Aのtoggleを開くと`aria-expanded=true`、表示文字`−`となり、`data-nested-panel-for=["lattice","scope-split-live-20260809","A"]`の枠が`368 x 376`で表示された。
3. 表示枠内に実split lineage由来のA1「A1 独立責務を抽出する」とA2「A2 A1後の責務を抽出する」が描かれ、A1→A2の辺も画面で確認した。A1/A2の`parent_task_id`はいずれもAである。
4. toggleを閉じると`aria-expanded=false`、表示文字`＋`、枠の表示寸法`0 x 0`となった。再度開くと`368 x 376`へ復帰した。
5. 親なし負例Cは通常カード「C 膨張ゼロの負のコントロール」として表示され、Cに対応するnested toggle/panelは0件だった。ページ全体のnested toggleはAの1件だけだった。
6. SSE接続状態は`進捗: 最新`。browser consoleの`warn` / `warning` / `error`は0件だった。
7. 表示中の実live planはA1/A2を含む全task doneの状態まで追従した。

## focused test

```text
node --test test/todo-gantt-nested.test.mjs
tests 7
pass 7
fail 0
duration_ms 97.2015
```

通過内容には、再帰的な子DAG、子から親箱への依存射影、foldされた親のcontainer維持、空panel抑止、親なしlayout/SVGのbyte-identical互換、network非依存の再帰panel公開が含まれる。

## 判定

受入条件をすべて満たす。

- 実splitの`parent_task_id` lineageを親Aの内側へA1→A2として表示: 合格
- 1段以上の再帰nested box: 合格（focused fixtureでは2段再帰も合格）
- 親なしplanの既存表示不変: 合格（実C負例およびbyte-identical test）
- 公開`gantt serve`から操作可能: 合格

findingなし。
