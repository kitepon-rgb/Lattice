# r3 決定25の再改訂

## 実施

- Peertable `docs/plan.md` の実行層統合契約を pull 型へ再改訂した。
- §4.8・§5・決定63・consumer contract §12.2 を、全モード共通の `claim` 主体へ統一した。
- 決定64として旧裁定1の機械 dispatch と、その後の「関所」解釈の撤回経緯を残した。
- 旧決定62は削除せず撤回済みの履歴として保持した。
- Peertable commit: `5c2017d` (`pull型の割当契約を正典化する`)

## 確認

- `git diff --check -- docs/plan.md`: green
- 生きた契約から `task 選択=Lattice` / 候補席配車 / `[受諾]`・`[辞退]` を除去し、残存は決定64の撤回理由と旧決定62の履歴だけであることを `rg` で確認した。
- r1 の確定契約と照合し、`claim`→`todo start`→席自身の `run intake` / `attach`→`todo done`→`run intake accept`、bridge read-only、未 claim task 自動選択なしを明記した。
- Peertable working tree の `skill/scripts/launch-seat.sh` は r2 の未完了差分であり、変更・stage・commitしていない。

## 配布

- Peertable 正典により push はオーナーの明示指示時だけのため、commit まで。push は実施していない。
