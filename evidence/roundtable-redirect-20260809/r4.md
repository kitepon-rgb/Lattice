# r4 pull型の実卓受入

## ① 席自身のclaim/startと設備取得

- `suzune` がroomでr4をclaimし、同actorでliteral `todo start`（activation event
  `14ed7d9c…`）を記録した。装置による席選定・配車・着手許可は介在していない。
- 席自身がpull run `roundtable-redirect-r4-20260809-0717`へ`run intake --task r4`し、
  detached worktree `scripted-wt-f9e30f9e0374b5841b878170/tree`を受け取った。
- 初回のboundary holdでr1の公開復旧経路欠落を3件実再現し、修正後に同run・同taskを再intakeした。
  最終observeはmanifest schema v4、`owns[].creates=true`、hold_count=0、lease granted。
- refresh前後でpacket digest `f9e30f9e…`とworktree pathは不変、実効manifest digestは
  `994bc087…`から`8bc1ff10…`へ更新された。event_countは2から4へ増え、失敗経路もappend-onlyで残る。

## ② 2-ToDo planning conflict

`test/runtime-pull-intake-cli.test.mjs`の隔離fixtureを単独実行した。fixtureはtemporary git repo、
実Todo store、同一planの2件のliteral start、実pull run、2回のintakeを使う。先着Aはlease granted、
同じ`src/shared.mjs`を宣言した後着Bは`planning_conflict`、lease withheld、holdとなる。

```text
node --test --test-name-pattern='後着planning conflict' test/runtime-pull-intake-cli.test.mjs
tests 1 / pass 1 / fail 0
```

自動選択はなく、hold中intakeが残るrunのcloseも`RUN_NOT_COMPLETE`で拒否される。

## ③ 配車surfaceの不在

Peertable現行実物の独立監査で、member/charterは席自身のclaim→start→intake、bridgeはpull runの
read-only観測と着手席宛holdだけを持つことを確認した。席選定、device assignment、work reportの
書込みsurfaceは無い（room Akari [1054]）。

## gate

- pull focused test: 12/12 green。
- syntax check: 148 files green。
- live①の独立observe: room Akari [1100]通過。
- live①②の最終独立監査: room Tsumugi [1150]。実run event列と2つの負対照を確認し、
  defect-freeで通過。
- ③の最終独立監査: room Akari [1159]。Peertable current HEAD `a4ab240`のmember、charter、
  run-bridge、setup、teardown、生成roleを再確認し、defect-freeで通過。

## accept / close / landing

- canonical Todo evidence binding後、pull runはtask accepted、accepted head
  `5d14206b0233d1af11b0fd413e2213a17674ab95`を記録した。
- run closeは成功し、event列は`task_accepted`に続いて`run_closed`で終端した。
- accepted headはmainへmerge済みで、`git merge-base --is-ancestor 5d14206 HEAD`は成功した。
- `run landing`のremote基準は`not_landed`。このrepoはpushがオーナー明示時だけなのでpushせず、
  未許可のremote反映を成功扱いしていない。local mainへの着地だけを確認した。
