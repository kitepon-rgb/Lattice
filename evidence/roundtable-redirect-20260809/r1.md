# r1 pull型への組み替え: Lattice側

## 実装

- `run start --selection pull`を、起動時のtask列挙やbase固定を持たないpull専用run storeとして追加した。
- `run intake`で、同一active plan versionのliteral `todo start`、intake時canonical HEAD、witness／independenceを束縛し、isolated detached worktreeを先に供給してから`none`または`hold`を返すようにした。
- planning conflictは後着をholdし、runtime diffは宣言外変更・他active intakeとの競合を観測してleaseをrevokedにする。attach済みworkerへsignalする前はpid／process start identity／argv digestを毎回OSから再認証する。
- attach inputは生観測の`started_identity`と`argv_digest`だけを受け、canonical self-digestを消費者へ要求しない。run storeにはLatticeが独立再観測して構築したprocess identityとargv digestだけを保持する。raw argvを渡した場合も値を診断へ出さない。
- acceptをsame-version・same-actorのliteral `todo done` digestと独立worktree checkpointへ束縛し、landing receiptへaccepted headを投影した。closeはintake membershipだけを対象にし、0件は`RUN_EMPTY`、未完了は拒否、完了後は冪等にした。
- legacy run readerとのschema dispatchを一方向に分け、pull／legacy artifactの双方向混在と未知schemaをtyped errorで拒否した。pullへlegacy専用commandを渡した場合もlegacy readerへ入る前にmode mismatchへ閉じる。
- independence全体がunknownならtask局所unknownが空でもholdし、conflictと向き付きprecedenceの双方を後着intakeのplanning holdへ投影した。precedence predecessorがTodo正本で既にdoneかつ同runにintakeされていなければ、後続だけのopen membershipを許可する。同runにintake済みならTodo done後もruntime acceptまで後続leaseを解放しない。
- observe/statusの各intakeへstart actor bindingを投影し、bridgeがTodo journal内部構造を別読せず介入先を決められるようにした。

## 検証

- `node --test test/runtime-pull-intake-cli.test.mjs` — 10/10 green。pull start/intake、未start拒否、intake時HEAD、conflict／precedence planning hold、run外done済みpredecessorのopen membership、同run未accept predecessorのhold、independence outcome unknown、到達不能base hold、accept/landing/close冪等性、raw argv非永続化・非診断出力、start identity decoy、同pid二重attach、双方向mixed store／legacy専用command拒否を確認。
- `node --test test/runtime-conflict-cli.test.mjs test/runtime-landing-cli.test.mjs` — green。既存legacy conflict／landing契約の互換性を確認。
- `npm run check` — green。
- `git diff --check` — green。

## r4 fresh卓実測で見つかった公開入口の修復

r4 の fresh 卓 preflight で、pull command は実装済みなのに `lattice run --help` と
`lattice run start --help` が legacy run しか列挙せず、席が公開面だけから pull run を作れない欠陥を確認した。
`run --help` へ pull start、intake、attach、intervention、accept、landing を列挙し、各 subcommand help も
実際に受理する exact argument contract と一致させた。

- `node --test test/cli-help.test.mjs` — 5/5 green。namespace help の3入口と各 pull subcommand helpを確認。
- `node bin/lattice.mjs run --help` / `run start --help` / `run intake attach --help` — 正規構文を実表示。
- `npm run check -- --files src/cli-help.mjs test/cli-help.test.mjs` — green。

## r4 preflightで見つかった空runのterminal欠落を修復

pull start直後のintake 0件runは、`run close`が`RUN_EMPTY`、legacy `run abandon`が
`RUN_MODE_MISMATCH`となり、公開CLIからactive状態を終えられなかった。closeの既存event/resultは
空集合を正しく扱えるため、不正なguardだけを外した。空runのcloseは`intake_count: 0`、
`landing.landed: false`、`accepted_receipts: []`を返し、成果が無いことを成功へ化かさずterminalへ移す。

- `node --test test/runtime-pull-intake-cli.test.mjs` — 11/11 green。空runの初回close、冪等close、
  active listからの除外を公開CLIで確認。
- `npm run check -- --files src/runtime-pull-intake.mjs test/runtime-pull-intake-cli.test.mjs` — green。

## r4 live intakeで見つかったboundary hold再評価不能を修復

r4 の live pull runで、worktree供給後に`BOUNDARY_UNVERIFIED / declare_and_recompile`が返った。
指示どおりwitnessを宣言して再compileしても、同taskの再`run intake`は保存済みintakeをそのまま返し、
`run intake intervention`も保存済み介入を読むだけだったため、holdを解除する公開経路が無かった。

- 同actor・同activation/versionの再intakeは、最新independence/witnessからboundaryを再評価する。
- `packet`と`worktree_path`は初回intakeのequipment identityとして不変にする。append-onlyな
  `intake_refreshed` eventへ、実効`manifest`、independence/witness digest、interventionを一体で
  記録する。projection後のobserve/attach/acceptはこの実効境界の最新版を読む。
- 再compileのcommitでcompiled baseがintake baseより後になった場合、両SHA間の変更が宣言境界と
  非交差な時だけleaseを解放する。宣言境界に触れた変更があれば`version_drift` holdを維持する。
- independence schemaは文字列を直書きせず`TODO_INDEPENDENCE_SCHEMA`を参照し、v4以後の正本と同期する。
- witness v5からruntime manifestを作る時も`BOUNDARY_MANIFEST_SCHEMA`を参照し、`owns[].creates`や
  `lines`を旧v2へ誤投影して`BOUNDARY_UNVERIFIED`へ落とさない。
- checkpointのundeclared/overlap判定はimmutable equipment packetの初回scopeではなく、
  最新effective manifestの`writes`を正本にする。packetはexecutor identityの検査にだけ残す。
- 初回intakeのstrict start確認と既存intakeの再認証を分け、Todo done後にruntime holdが判明しても、
  保存済みliteral start/version bindingを照合して再intake・復旧できるようにする。

検証済み:

- Lattice `ab02356`（本体＋focused test）。
- `node --test test/runtime-pull-intake-cli.test.mjs` — 12/12 green。
  packet/worktree不変、最新manifest/digestへの置換、attach済みhold workerの停止→再開、
  witness v5の`owns[].creates`を含むcurrent runtime manifest、compiled-after-intakeの非交差release、
  境界交差の再holdを確認。
- `test/runtime-io-sentinel.test.mjs` / `test/runtime-line-observation.test.mjs`を含む関連45/45 green。
  packet.scopeが空でもlatest manifestがwriteを宣言したpathをundeclaredへ誤分類しない反例を固定。
  Todo done後のhold→recompile→再intake→worker resumeも同じfixtureで確認。
- `npm run check` — syntax check 147 files green。

live実測:

- redirect witnessをv4へ再compileし、outcome=`compiled`、unknown=0を確認した。
- live run `.lattice/runs/roundtable-redirect-r4-20260809-0717` の同一r4を再intakeした。
  `hold_count`は1→0、`event_count`は2→4（最初の旧schema変換失敗refreshもappend-only履歴に保持）。
- manifest digestは`994bc087…`→`8bc1ff10…`、witness/independence digestも最新版へ更新した。
  packet digest `f9e30f9e…` とworktree pathは不変だった。
- compiled baseが初回intake baseより後の29 path変更はr4宣言境界と非交差で、
  `base_relation=compiled_after_intake`、lease=`granted`、intervention=`none`へ遷移した。
