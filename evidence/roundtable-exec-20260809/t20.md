# t20 Lattice正典更新・0.52.0出荷

## 公開候補

- 版: `@quolu/lattice@0.52.0`
- 版数・CHANGELOG・Peertable consumer contract: `91c300f`
- consumer contractの実CLI訂正とstderr警告混入test harness修正: `7b6cca8`
- cross-plan dependencyのCHANGELOG追記: `ad8b80d`
- t20最終boundary witness: `c5f0a01`
- cross-plan backlogの完了状態訂正（親所有・全列対象）: `81771f2`
- 0.52.0へ入る製品変更:
  - 宣言scope膨張のtask別split推薦: `4ce0eed`
  - plan跨ぎ依存の発見時接続、ready／frontier／active席数反映、公開Gantt、統合受入:
    `7fbc063`、`963d27a`、`a52b64b`、`8cf3bed`

## consumer contract

- Peertableを外部消費者として、既存の汎用面を4責務へ同期した:
  ①境界付きready／着手助言 ②claim後の実行設備／介入
  ③結果／証跡／着地の束縛 ④監査状態／公開観測。
- `external_pane`はidentity文書の任意書込例外であり、第5の実行面やPeertable専用schemaではない。
- Peertable側t21との突合で、存在しない`run selection --mode pull`を公開前に検出した。
  Lattice側は`7b6cca8`で実CLI
  `run start --selection pull --id <id> --plan <key> --equipment detached-worktree`へ訂正し、
  Peertable側のDecision 69／§12と一致させた。

## boundary compile

- 最終宣言は`CHANGELOG.md`、`docs/00_product-contract.md`、版数2path、witness generator、
  release gateで変更したtest 2path、この証跡を所有する。
- base `c5f0a01d1e70f4c1bacc5a3826173d7eebdad220`でcompile成功。
  task 1、conflict 0、unknown 0、result digest
  `86d278e5530d7e55cd7970bcec2322cf83e3f669caf3c89ddbb1122bd5db064b`。
- scope expansion推薦は2回・6path→8path・`gate_shape=false`。追加はいずれもrelease gateで露見した
  test harness追従で、version・公開契約・release gateを分ける独立deliveryではない。
  t20の原子的な出荷受入を壊すためsplitは採らない（room [2269]）。

## 検証

- cross-plan campaignの独立terminal監査: findingなし、phase accept `e9545d6`。
- 完全CI: clean SHA `1faee295c1d316fd1028157af824e4734654ba8c`でexit 0。
  product 1550/1550、sensor、全static gate、store verify green（room [2291]、Suzune実物照合 [2292]）。
- 完全CI初回は2件を成功へ丸めずrc1として扱った。exact advisory key漏れは`1faee29`の1行で修正。
  runtime work-order testは単独2回greenで一過性report visibility競合と判定し、source変更を行わなかった。
- t20 focused: `todo-independence-cli`、`todo-store-atomic-commit`、`todo-cli`の55/55 green。
- `npm run check`: syntax 150 files green。
- clean HEAD `81771f2e6c9f8747cabb27f03f9f18c701103275`で`npm pack --dry-run --json`:
  800 files、size 7,391,057、unpacked 77,684,555、shasum
  `69f61008c095b032a2d97d8bda8fdd10fe628a51`、integrity
  `sha512-+JWwhutjxhYjmjNDJZ/J3fALXWXQ/DVeJM9SU79wYGKCJBQpo0PzzYZesKR9mxfEg4AqCrOHvvPt8t80pWxcDA==`。
  campaign evidenceはtarball対象外であることもfile listから確認した。

## 公開・実機受入

- 独立監査はfindingなし（room [2305]）。Bell承認 [2309] と34コミット全件の実物確認後、
  `main`を通常pushした。公開候補`e98e7b47b42452a197045cffd68c98be0dc1a85a`は
  `origin/main`と一致し、`git merge-base --is-ancestor e98e7b4 origin/main`はrc0。
- `npm publish`で`@quolu/lattice@0.52.0`をpublic/latestへ公開した。registry実測は
  version `0.52.0`、shasum `69f61008c095b032a2d97d8bda8fdd10fe628a51`、integrity
  `sha512-+JWwhutjxhYjmjNDJZ/J3fALXWXQ/DVeJM9SU79wYGKCJBQpo0PzzYZesKR9mxfEg4AqCrOHvvPt8t80pWxcDA==`で、
  clean packのbytes証跡と完全一致した。Rin [2321] とBell [2322] の独立照合も一致。
- `npm install -g @quolu/lattice@0.52.0`を実行し、実体は`/opt/homebrew/bin/lattice`、
  `lattice --version`は`0.52.0`。
- 配布CLIの`lattice factory-diagnostics --json`は
  `lattice.native_factory_diagnostics.v1`／overall `ok`。package version、Node runtime、CLI surface、
  MCP entry、sensor attributionの5検査が全て`ok`。
- 配布CLIで実storeを読み、`lattice.todo_status_result.v6`、activeはt20のみ、readyは0を確認。
  `cross-plan-dependencies-20260809`のcoordinationはmode `witness`として投影された。
- 配布CLIでt20 witnessを再compileし、base `e98e7b4`、task 1、conflict 0、unknown 0、
  scope split推薦`scope_expanded_consider_split`（growth 2回、6→8 path、gate shapeなし、
  next action `consider_todo_split`）を再現した。2回の膨張はともに同じ0.52.0 release gateの
  follow-upで、t20は出荷を原子的に閉じるため分割しない既裁定を維持する。

## 最終判定

- 版数・契約・CHANGELOG・boundary・focused test・完全CI・pack bytes・registry bytes・
  global install・配布CLI smokeが一致したため、t20を受入可能と判定する。
- この最終証跡blobをt20 doneへ束縛する。
