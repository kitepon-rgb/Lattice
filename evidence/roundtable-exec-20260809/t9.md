# t9 決定25改訂の正典追記

## 結論

Peertable の managed run だけを、旧「room の先行 claim が割当」から受諾表明プロトコルへ改訂した。
task 選択は Lattice、候補席の選択は bridge、席との束縛は `[受諾]` が持つ。`[配車]` は提示の可視化で
あって割当ではなく、`[辞退]` は別席への再配車入力である。managed run に載せない Lattice 併用卓と
単独円卓の claim/join は維持した。

## 境界宣言

- witness set: `.lattice/todo/witness/roundtable-exec-20260809.json`
- initial witness commit: `8601275`（`pt-plan-md` / `pt-member-md` を越境資源として宣言）
- 監査補修後の generator commit: `1f3d7c2`（配布入口 `pt-skill-md` も越境資源へ追加）
- t9 / t11 / t18 combined witness commit: `ad37453`
- independence compile: `coverage=verified`、`conflict_count=0`、`unknown_count=1`
- unknown の内容: Lattice sensor が Peertable repo を観測しない `external_repo` 境界。未宣言ではなく、
  越境作業であることを明示した unknown である
- compile 時の exact base: `871a4818c75ee1d334b270bed16b1f94b9f33056`

## 変更

Peertable commit `0236b8f`（`managed runの割当を受諾表明へ改訂する`）。

- `docs/plan.md`
  - §4.8 と動作フローを3層の受け渡しへ改訂
  - consumer contract §12.2 で todo event の actor を assignee と読まないことを明記
  - 決定62を追加し、決定25を managed run 経路だけ改訂
- `skill/templates/charter.md`
  - managed run は `[配車]` → `[受諾]` / `[辞退]`、その他の卓は claim/join という分岐を憲章化
- `skill/templates/member.md`
  - 通常の claim/start/done ループと managed run の work order ループを分離
  - work order の7欄を読んで受諾・辞退し、claim や todo start を割当の二重記録として重ねない作法を明記
  - 再着任時は `run observe` と room の配車語彙から状態を取り直す

独立監査で、配布入口 `skill/SKILL.md` が旧「自分で取るだけでなく run から配車」と教えたままで、managed run
でも self-claim を併用できる読みになる不一致が見つかった。Peertable commit `f1776ba` で同入口も
「managed run は claim で取り合わず、task 選択=Lattice / 候補席=bridge / 受諾=席」へ同期した。

`skill/templates/member-standalone.md` は変更していない。単独円卓では room の claim が工程正本なので、
managed run の規律を流し込むと既存契約を壊すためである。

## 検証

1. `PEERTABLE_URL= node room/client.mjs diagnostics --json`
   - `version_consistency=pass`
   - `bin_integrity=pass`
   - `node_runtime=pass`
   - `skill_bundle=pass`
   - `room_reachability=not_applicable`
   - `overall=ready`
2. canonical assertion（Node の一時 check）
   - 3正本すべてに `[配車]` / `[受諾]` / `[辞退]` / managed run / `[claim]` が存在
   - 憲章に「この経路で `[claim]` を重ねない」
   - member に3層分割
   - plan に決定62と非 managed-run 互換を確認
   - 結果: `t9 canonical assertions: pass`
3. `git diff --check`: pass
4. Peertable working tree: commit 後 clean

## 独立監査

kanade が初回3正本と補修後の4面を実diffから確認した。旧 charter を負の対照にすると非二重帳簿文が無く失敗し、
現行では検出が通ること、`member-standalone.md` は親commitから差分ゼロで claim 契約を維持すること、diagnostics
`overall=ready` と diff-check green を確認。room [470] で finding 解消・受入可となった。

## 監査で見てほしい点

1. 共通憲章のモード分岐が、単独円卓と非 managed-run の claim/join を残したまま managed run の二重帳簿だけを消しているか
2. `[配車]` を提示、`[受諾]` を束縛、`[辞退]` を再配車入力とした語義が docs / charter / member で一致しているか
3. member の「campaign todo を別途閉じる場合」は receipt 確認後の工程手続きであり、席割当の二重記録へ戻っていないか
