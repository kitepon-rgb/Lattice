# t11 Wave 2受入: 2席・3taskの競合配車

- 実測・裁定・証跡: akari（あかり）
- 実席: kanade（t11a / t11b）、tsumugi（t11c）
- 日付: 2026-08-09
- run: `.lattice/runs/t11-wave2-1`
- base: `64ccf75eb4eff39ab11a2c77fe8a36d0a3ead3a2`
- request digest: `9f2814ff6642f71e5407b02e4b1bd143e60de44f18cb7f0d063b9900fc769c5c`
- plan digest: `41eaef3fdd973ca50ac57cedcb68061496475c8fd2875032d18a8993129469dc`

## 受入結果

| 条件 | 結果 | 直接証拠 |
|---|---|---|
| 2席以上・3task以上で、planのconflict対を同時dispatchしない | **達成** | 2席、3task。`dispatch_decided` は seq 2 `[t11a,t11c]` と seq 13 `[t11b]` の2回だけ |
| 受諾表明レース `invalid_start_transition` が0件 | **達成** | runのcontrol/event/metaとbridge runtime logを対象限定検索して0件。roomにも実エラー報告0件 |
| 同一fileへ書く2taskの片方が、もう片方のdispatch中に待つ | **達成** | t11a dispatch seq 3、terminal seq 9、accepted seq 12。t11bはその後のdecision seq 13、dispatch seq 14 |

最終statusは `accepted:[t11a,t11b,t11c]`、`running:[]`、`closed:true`。`lattice event verify` は
15 checks / failed 0、`run close` は `outcome:closed` / event count 20だった。

## requestとplan

t11aとt11bは同じpath `evidence/roundtable-exec-20260809/t11-shared.txt` を書き、共有資源
`t11-shared-file` を宣言した。t11cは独立path `t11-independent.txt` だけを書く。capacityは2。

最初の試案は同じ`writes`と同じ`owns`だけを宣言し、compile結果がconflict 0、wave
`[t11a,t11b] → [t11c]`になったため、**そのrequestではrunを開始しなかった**。現行front-endの契約を実読し、
bare shared resourceを追加した最終requestでは次を得た。

```text
conflicts: [{ todo_ids:[t11a,t11b], resource_id:t11-shared-file }]
waves: [t11a,t11c] -> [t11b]
minimum_feasible_waves: 2
```

## event chainによる非重複の証明

```text
seq  2 dispatch_decided       [t11a,t11c]
seq  3 executor_dispatched    t11a
seq  4 executor_dispatched    t11c
seq  6 executor_terminal      t11c
seq  7 checkpoint_observed    t11c
      status: running=[t11a], accepted=[], dispatchable=[]
seq  9 executor_terminal      t11a
seq 11 receipt_accepted       t11c
seq 12 receipt_accepted       t11a
seq 13 dispatch_decided       [t11b]
seq 14 executor_dispatched    t11b
seq 16 executor_terminal      t11b
seq 18 receipt_accepted       t11b
seq 19 run_closed
```

全`dispatch_decided`を機械抽出し、t11aとt11bを同時に含むpayloadが無いことを`jq -e`で確認した。
t11c完了後もt11aを意図的にrunningへ保持し、statusが`dispatchable:[]`の間はt11bが配車されなかった。
t11aの受理後に初めてt11bがdispatchされたので、単なる最終event一覧でなく、実際の待機窓も測れている。

## 2席と実diff

room / bridgeの対応は次の通り。

```text
[配車] t11a -> kanade  / [受諾] / [完了]
[配車] t11c -> tsumugi / [受諾] / [完了]
[配車] t11b -> kanade  / [受諾] / [完了]
```

Latticeが作ったreceiptの`observed_diff`は全taskで1pathだけだった。

| task | receipt | observed diff | receipt head |
|---|---|---|---|
| t11a | `receipt-cad078b66603a2f9670e212c` | added `t11-shared.txt` | `99bef60459d793635ce63dbe2af2f727b9fc1cbe` |
| t11b | `receipt-ac5a1323d7a2fbaa70a42531` | added `t11-shared.txt` | `c78f39624cf5dbf1a141b266bea42ab67f9c9099` |
| t11c | `receipt-51772f206bbfea8c473ff21c` | added `t11-independent.txt` | `dfd7bcb32176b49a9ffa108b1e41f184ee3ac831` |

各隔離worktreeは指定1fileだけのcommitでclean、canonical treeはHEAD不動・cleanだった。closeに同梱された
landing reportは3receiptとも`not_landed / landed:false`、repositoryは既定branch resolved、unpushed commits 0。
隔離worktreeのcommitがcanonicalへ統合されていないことを、成功へ丸めず公開状態値で確認できた。

## `invalid_start_transition` の測定範囲

最初の監査コマンドはrun directory全体を再帰検索し、隔離worktreeに複製された`src/todo-store.mjs`の
エラー定数とtest fixtureを拾って`present`と誤表示した。これはruntime発生ではなくsource textである。
測定器を次へ限定して再実行した。

- `events.json` / `control-events.json` / `control-request-ledger.json`
- `committed-epoch.json` / `release-epoch.json` / `run-meta.json`
- `.team/run-bridge.log`
- 今回run開始以後のroom発言

結果はruntime log 0件、roomの実エラー報告0件。room内の文字列出現1件は、akari自身の
「実発生0」という監査報告だけだった。

## run前後の中断と回復

1回目のbase候補`da97f9d`を凍結した直後、親の非交差docs commit 2本でHEADが`64ccf75`へ進み、
`plan compile`は`STALE_BASE`でrun生成前に停止した。2commitのpath非交差を確認し、combined independenceを
新HEADへcompileし直し、親へ直接凍結を届けた上で最終runを開始した。失敗run storeは生成されていない。

run開始前にはroom server入替も発生した。bridgeはSSE切断の2秒後に自動再接続し、`since=512`で
取りこぼし0を確認した。再接続・HEAD安定・2席readyを確認してからrunを開始している。

## t11が露出したt8の実欠陥

bridgeの補助進行観測は、absolute run directoryを公開CLIの`--run`へ渡し、次をloudに記録した。

```text
INVALID_RUN_REF: run refは `.lattice/runs/<run-id>` のrepo相対形式でなければならない
```

配車・受諾・report・receiptの本線は完走し、t11の3受入条件はevent chainで独立に満たした。一方、t8が約束した
`[run]`進行返却は実環境で未達なので、t8の実装欠陥として採用した。Peertable `3ab06a8`でproject-relative
run refとcwd固定へ直し、stubへabsolute refを`INVALID_RUN_REF`で拒否する負側を追加した。欠陥版は7件NG、
修正版は11/11 green。Lattice側の追記はevidence `787f0a7`、note `1ceae9a`。

## 後片付けの実測

cleanup前は2席bridge pid 15714、3隔離worktree、spoolにorder 3件＋report 3件が残っていた。最初のspool確認は
`find -maxdepth 1`で下位のorders/reportsを見落とし、誤って0と読んだ。全階層を測る形へ直し、一覧を固定してから
次を実施した。

- descriptorと現processのstart identity・script・project path一致を再認証し、正規`--stop`（rc 0）
- receipt headとclean statusを照合してから、t11所属の3 worktreeだけ撤去
- spoolのorder 3件＋report 3件を撤去

最終再測定は次の通り。

- bridge / supervisor / controller / run所属process: 0
- t11所属worktree: 0
- spool: 0 file（全階層）
- orphan test daemon: matched 0 / reapable 0 / still_alive 0
- canonical status: 本証跡1fileだけ
