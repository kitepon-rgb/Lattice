# t21 Peertable正典更新・出荷

## 正典と版

- Peertable `docs/plan.md`へ決定69を追加し、Lattice実行層のtask別消費はroomのclaimと
  literal `todo start`後に始まること、`run close`と`run landing`を別の終端として扱うことを固定した。
- §12のconsumer contractを4面のまま実態へ同期した: ①境界付きready／着手助言
  ②claim後の実行設備／介入 ③結果／証跡／着地の束縛 ④監査状態／公開観測。
  `external_pane`はidentity文書の唯一の書込例外のままで、新しい面やPeertable専用schemaを増やしていない。
- §12.5のstandalone比較も同じ4面へ揃え、Lattice不在でもroomと宣言claimが残る相互独立を維持した。
- release candidate: `cd0a3a8`、正規pull入口の追補: `022236c`、standalone表同期: `8ea9ddb`。
  公開完了記録を含むPeertable最終headは`41082a508cda40ab93246be7fae5ae9266f95175`。
- Lattice側契約との突合で、存在しない`run selection --mode pull`を公開前に発見した。
  実CLIとhelp/testから正規入口
  `lattice run start --selection pull --id <id> --plan <key> --equipment detached-worktree`を確認し、
  Lattice側は`7b6cca8`、Peertable側は`022236c`で一致させた。

## 独立監査

- 文脈近接の独立監査席Yuzuが`cd0a3a8`を実物照合しfindingなし（room [2254]）。
- `022236c` deltaもfindingなし。共有run準備とtask claimの順序を分離し、正規CLIが実装契約と一致
  （room [2258]）。
- `8ea9ddb` deltaもfindingなし。§12.5が新4面と1:1対応し、最終範囲
  `cd0a3a8^..8ea9ddb`を受入可（room [2261]）。

## 出荷前gate

- `node --check room/client.mjs` / `node --check room/server.mjs`: green。
- `PEERTABLE_URL= node room/client.mjs diagnostics --json`: `overall=ready`、
  version/bin/node/skill pass、room reachabilityは意図どおりnot applicable。
- 負のコントロール: 一時コピーの`package.json`だけを`9.9.9`へずらすと
  `version_consistency=fail`、`overall=not_ready`、exit 1。検査が版数driftを捕捉できることを確認した。
- `npm install --package-lock-only --ignore-scripts`: lock整合、audit 0 vulnerabilities。
- `npm pack --dry-run --json` / `npm publish --dry-run --json`: 27 files、shasum
  `16697a868799b7d2f64d1d6ef70c82a2aae53d32`、prepublishOnly diagnostics ready。
- Bellが通常push、npm publish、global install、公開記録pushの4操作を明示承認（room [2266]）。

## 公開と実機受入

1. Peertableのrelease対象`8ea9ddb`を通常pushし、
   `git merge-base --is-ancestor 8ea9ddb origin/main`がexit 0、remote headも同commitであることを確認した。
2. `npm publish`で`peertable@0.3.5`を公開した。registry再読結果は
   `latest=0.3.5`、shasumは上記dry-runと同一、integrityは
   `sha512-KZHfMur5Zcx4gwZHfeGrB4BrFVSg7LdQlLQ0zJzQtUOiXzsOHhpxl90Brds6OJRufhgze/xccBq0p2PEPDI7rg==`。
3. `npm install -g peertable@0.3.5`後、`/opt/homebrew/lib`に同版、
   `/opt/homebrew/bin/peertable-client`と`peertable-room`を確認した。インストール物の
   `peertable-client diagnostics --json`は`overall=ready`。
4. `docs/plan.md`を公開済みへ更新した`41082a5`を通常pushし、local `HEAD`と`origin/main`が
   `41082a508cda40ab93246be7fae5ae9266f95175`で一致、working tree cleanを確認した。

## 判定

t21の正典更新、Lattice側との契約整合、独立監査、npm公開、registry bytes照合、global install実測、
公開記録のremote着地まで完了した。未実施をgreenへ丸めた項目はない。
