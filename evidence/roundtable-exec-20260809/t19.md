# t19 managed run実戦受入

## 対象

- run: `t19-scope-split-20260809-2134`
- 外側plan / task: `scope-split-20260809` / `s6`
- 実戦中に作成・splitしたplan: `scope-split-live-20260809`
- split successor: `rev-e5ef810120268b23c8b3f240`
- 公開Gantt実ブラウザ受入: `scope-split-viz-20260809` / `v2`

## managed runの終端

1. pull intakeはactor `rin`、base `1141e21358c2af61559b023774969ddbb8c565fb`、
   隔離worktreeを束縛してlease `granted`となった。
2. s6のliteral `todo done` event
   `d65732cda4e824b2473e12519bcccdca972df904c662a2ca4d0916e8157c94ab`を、
   accepted head `6c96c35708d73f836a35f6619a5defa858e0ecf3`と独立checkpointへ束縛して
   intake acceptした。accept resultは
   `74f8a3661e5c9c35d2e08df45bc40124e68a5ceb527da5ed138f1c4615bc9877`。
3. run closeは`accepted_count=1`、`closed=true`、`event_count=8`。
4. accepted headを含むmainをforceなしで既定remote branchへpushした後、独立に
   `lattice run landing`を再実行した。`landed=true`、`unpushed_commits=0`、
   receiptの`landing_state=landed`、result digest
   `7f04e7fe257ddb4658c2600328c5bb5b4991f27e49917d7a65af4886706ec762`。
   `git merge-base --is-ancestor 6c96c357… origin/main`もexit 0だった。

## 摩擦1〜6との突合

1. **編集途中treeの混入**: s6はbase固定の隔離worktreeだけで変更・検証し、受入前後のcanonical
   treeはcleanだった。別席の編集中treeを掴んでいない。
2. **偶然の共有tree検査への依存**: s6はSuzuneの独立監査、v2はYuzuの実ブラウザ受入として
   意図的な検査面を通した。共有working treeを偶然起動したことを検査に数えていない。
3. **roomだけの直列化**: s6の実planはwitnessをcompileし、split後の依存を
   `A1→A2`、`A1→A`、`A2→A`、`A→B`としてstoreへ固定した。着手順はstoreのready/frontierで
   制御し、会話上の宣言だけに依存していない。
4. **claimとstartの隙間**: pull intakeは既にstart済みのliteral task identity・actor・baseを検証し、
   accepted receiptをそのdone eventへ束縛した。別taskや別actorの自己申告を完了へ丸めていない。
5. **path外の共有protocol依存**: 今回の実planに共有protocol変更は無かった。既設のline資源契約を
   省略したという成功主張はせず、宣言済みwrites/reads/affected testsをcompileしてunknown 0・conflict 0を
   確認した。実戦で新たに露出したplan跨ぎの受入依存は、線が存在しないままactiveに見える欠陥として
   `docs/plan_backlog.md`へ記録し、t19へ無断追加せず次campaignへ渡した。
6. **未着地成果の取り残し**: close直後は57 commits未pushのためlandingが正しく`false`を返した。
   通常push後に同じrunを再観測し、`landed=true`と未push0まで確認した。未着地を成功扱いしていない。

## 個別受入証拠

- split、lineage、負control、後方互換: `evidence/scope-split-20260809/s6.md`
- 公開Ganttの実ブラウザ操作とfocused test: `evidence/scope-split-viz-20260809/v2.md`
- s6独立監査: findingなし。focused `todo-independence-guidance` / `todo-split`は28/28 green、
  `git show --check` green。
- v2実ブラウザ受入: nested panel、A1→A2、collapse/reopen、親なしC、SSE最新、console error 0、
  focused test 7/7 green。

## 判定

t19のmanaged run、両受入面、accept、close、remote landingを実測で完走した。摩擦1〜6を
成功へ丸める再発はない。実戦で発見したplan跨ぎ依存の欠落は別campaignで扱う。
